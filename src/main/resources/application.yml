server:
  address: localhost
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/murange2?serverTimezone=Asia/Seoul
    username: root
    password: 401868
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto:  update # create
    properties:
      hibernate:
        show_sql: true
        format_sql: true