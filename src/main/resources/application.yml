server:
  address: localhost
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/newmurange?serverTimezone=Asia/Seoul
    username: root
    password: nalgae415
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER # Swagger
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto:  create   #  create validate update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        globally_quoted_identifiers: true # 예약어 사용 허용